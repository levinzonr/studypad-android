<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="cz.levinzonr.studypad.presentation.screens.sharinghub.suggestions.NotebookSuggestionsFragment">

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/suggestionsRv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>


    <cz.levinzonr.studypad.presentation.common.EmptyStateView
            android:id="@+id/emptyView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/reviewBtn"
            android:layout_width="wrap_content"
            android:layout_margin="@dimen/space_large"
            android:text="Review"
            app:icon="@drawable/ic_thumbs_up_down_black_24dp"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
