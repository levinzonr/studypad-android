<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">


    <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/challengeIcon"
            app:elevation="1dp"
            android:clickable="false"
            android:focusable="false"
            app:fabSize="mini"
            android:src="@drawable/ic_close_black_24dp"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginBottom="@dimen/space_big"
            android:layout_marginTop="@dimen/space_big"
            app:layout_constraintStart_toStartOf="parent"/>


    <TextView
            android:textColor="#000"
            android:text="Challenge"
            android:id="@+id/challengeTv"
            android:layout_width="0dp"
            android:textSize="15sp"
            android:layout_marginStart="@dimen/space_normal"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/challengeIcon"
            app:layout_constraintTop_toTopOf="@+id/challengeIcon"/>


    <TextView
            android:id="@+id/challengeSubTv"
            android:text="params"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@+id/challengeTv"
            app:layout_constraintBottom_toBottomOf="@+id/challengeIcon"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="@+id/challengeTv"/>

</androidx.constraintlayout.widget.ConstraintLayout>